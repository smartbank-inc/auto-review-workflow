{
  "name": "auto-review-workflow",
  "version": "1.0.0",
  "description": "Reusable GitHub Actions workflow for automatic PR review evaluation",
  "private": true,
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
